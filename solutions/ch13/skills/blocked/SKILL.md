---
name: blocked
description: A skill that exists but is never declared loadable by any other skill.
type: loadable
tools: run_command
---
# Blocked Skill

This skill is registered but never appears in any loaded skill's
loadable-skills list, so it cannot be loaded via load_skill.
